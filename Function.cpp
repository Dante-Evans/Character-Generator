#include <iostream>
#include "Function.h"
#include <vector>

void playerStats(std::vector<charStats> &arr, int n)
{
	for (int i = 0; i < n; i++)
	{
		//std::cout << arr[i].statName << " " << arr[i].statValue << "\n";
		arr.size();
	}
}